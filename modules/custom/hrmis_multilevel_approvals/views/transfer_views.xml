<odoo>
    <!-- Form View -->
    <record id="view_hr_employee_transfer_form" model="ir.ui.view">
        <field name="name">hr.employee.transfer.form</field>
        <field name="model">hr.employee.transfer</field>
        <field name="arch" type="xml">
            <form string="Employee Transfer">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="employee_id"/>
                        <field name="from_department_id"/>
                        <field name="to_department_id"/>
                        <field name="state" readonly="1"/>
                    </group>

                    <notebook>
                        <page string="Approvals">
                            <field name="approval_status_ids" readonly="1">
                                <tree>
                                    <field name="user_id"/>
                                    <field name="sequence"/>
                                    <field name="approved"/>
                                    <field name="approved_on"/>
                                    <field name="comment"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>

                    <footer>
                        <button name="action_submit" type="object" string="Submit" class="btn-primary"
                                attrs="{'invisible': [('state', '!=', 'draft')]}"/>
                        <button name="action_approve_by_user" type="object" string="Approve" class="btn-success"
                                attrs="{'invisible': [('state', '!=', 'submitted')]}"/>
                        <button name="action_reject" type="object" string="Reject" class="btn-danger"
                                attrs="{'invisible': [('state', '!=', 'submitted')]}"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <!-- List / Kanban View -->
    <record id="view_hr_employee_transfer_list" model="ir.ui.view">
        <field name="name">hr.employee.transfer.list</field>
        <field name="model">hr.employee.transfer</field>
        <field name="arch" type="xml">
            <tree string="Employee Transfers">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="from_department_id"/>
                <field name="to_department_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Action -->
    <record id="action_hr_employee_transfer" model="ir.actions.act_window">
        <field name="name">Employee Transfers</field>
        <field name="res_model">hr.employee.transfer</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create employee transfers here.
            </p>
        </field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_hr_employee_transfer_root" name="Transfers" parent="hr.menu_hr_root"/>
    <menuitem id="menu_hr_employee_transfer" name="Employee Transfers" 
              parent="menu_hr_employee_transfer_root" action="action_hr_employee_transfer"/>
</odoo>
