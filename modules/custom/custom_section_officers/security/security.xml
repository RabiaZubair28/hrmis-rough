<odoo>
    <!--
    NOTE:
    This module's UI checks membership in `custom_login.group_section_officer`.
    Access rights for that group are granted via `security/ir.model.access.csv`.
    -->
</odoo>
